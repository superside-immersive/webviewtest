# Assets for Page 2: VRM Body Tracking

Place the following file here:

## 3D Model
- `character.vrm` — VRM model with a standard humanoid skeleton

The model must include the standard VRM humanoid bones (`hips`, `spine`, `chest`, `head`,
`leftUpperArm`, `leftLowerArm`, `rightUpperArm`, `rightLowerArm`,
`leftUpperLeg`, `leftLowerLeg`, `rightUpperLeg`, `rightLowerLeg`, etc.).
